# Reproducible optional dependencies for real Phase 18 local/free-GPU visual proof.
#
# PyTorch is intentionally not pinned here: Colab supplies the CUDA build and
# Phase 18 proves CUDA/BF16 support at runtime instead of replacing torch.
#
# The semantic stack IS pinned. A live Colab notebook can become internally
# inconsistent when Pillow is upgraded across a major release while modules are
# already imported. The verified Phase 18 Python 3.13 Colab baseline is Pillow
# 11.3.0 + Transformers 4.56.2. Keep both exact until a deliberate compatibility
# qualification updates this lock.
#
# Diffusers is constrained to the verified FLUX.2-capable line. Other libraries
# are bounded to avoid accidental future major-version drift.
diffusers>=0.39.0,<0.41.0
transformers==4.56.2
accelerate>=1.10.0,<2.0.0
safetensors>=0.6.0,<1.0.0
huggingface_hub>=0.34.0,<2.0.0
Pillow==11.3.0
